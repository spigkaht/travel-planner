<%= simple_form_for(@city, wrapper: :tailwind_form) do |f| %>
  <%= f.input :name %>
  <%= f.input :country, as: :string %>
  <%= f.button :submit, class: "bg-blue-500 hover:bg-blue-600 text-white font-bold py-2 px-4 rounded" %>
<% end %>
