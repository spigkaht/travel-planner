<div class="grid grid-cols-2 h-screen">
  <div class="bg-[url('/images/forest-road-above-2000px.jpg')] bg-no-repeat bg-cover bg-center">
  </div>
  <div class="flex flex-col w-full mt-20 mx-auto px-28">
    <h2 class="mb-8 font-bold">Sign in to Travel Planner</h2>
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), wrapper: false) do |f| %>
      <div class="mb-4">
        <%= f.label :email,
                    class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.input_field :email,
                    required: false,
                    autofocus: true,
                    class: "block w-full rounded-lg border border-green-500 focus:border-green-700 focus:ring-green-700 p-2",
                    input_html: { autocomplete: "email" } %>
      </div>
      <div class="mb-4">
        <%= f.label :password,
                    class: "block text-sm font-medium text-gray-700 mb-1" %>
        <%= f.input_field :password,
                    required: false,
                    class: "block w-full rounded-lg border border-green-500 focus:border-green-700 focus:ring-green-700 p-2",
                    input_html: { autocomplete: "current-password" } %>
      </div>
      <% if devise_mapping.rememberable? %>
        <div class="flex items-center mb-4">
          <%= hidden_field_tag "user[remember_me]", "0" %>
          <%= check_box_tag "user[remember_me]", "1", false, class: "h-4 w-4 text-green-600 bg-green-500 border-gray-300 rounded" %>
          <%= label_tag "user_remember_me", "Remember me", class: "ml-2 block text-sm text-gray-900" %>
        </div>
      <% end %>
      <div class="flex flex-col items-center">
        <%= f.button :submit, "Log in", class: "bg-gray-50 border border-gray-300 text-gray-900 h-8 text-sm rounded-lg hover:ring-green-500 hover:border-green-500 w-40 p-1.5 my-1" %>
        <%= render "devise/shared/links" %>
      </div>
    <% end %>
  </div>
</div>
